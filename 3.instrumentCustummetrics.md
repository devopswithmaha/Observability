

# Observability 


###  devops and SRE
- prometheus
- Garafana
- ELK
- Jaeger 

1) Node_exporter
2) DB_exporter
3) kube-state

### developer
4) instruments   like how login & reg  from 30day  on app
metric type
- counter -> EX:  increase login  from 30days
- cauge   -> EX:  increase & decrement  ( like cpu utilization)
- histogram-> http requtest duration as buket> 5s, buket>10s buket>105
- summary -> duration of HTTP requests in seconds'

## build image
```bash
# Dockerize microservice - a
docker build -t <<NAME_OF_YOUR_REPO>>:<<TAG>> application/myapp-a/ 
# or use abhishekf5/demoservice-a:v

# Dockerize microservice - b

docker build -t <<NAME_OF_YOUR_REPO>>:<<TAG>> application/myapp-b/ 

or  use

maha4iac/myapp-a:1
maha4iac/myapp-b:1 
```

## Kubernetes manifest
Apply the Kubernetes manifest files to your cluster by running:
```bash
kubectl create ns dev

kubectl apply -k kubernetes-manifest/
```

## Test all the endpoints
Open a browser and get the LoadBalancer DNS name & hit the DNS name with following routes to test the application: 
    - `/`
    - `/serverError`
    - `/notFound`
    - `/logs`
    - `/example`
    - `/metrics`
    - `/call-service-b`

## altertmanagement
echo "rtyh jzdb uzki rqjn" | base64

rtyh jzdb uzki rqjn

## cd  alerts-alertmanager-servicemonitor-manifest
kubectl apply -k  .
